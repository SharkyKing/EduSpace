import React from 'react';
import './Comment.css';

const Comment = ({className, children, onClick, type = 'button', disabled = false }) => {
    return (
        <>
        </>
    );
};

export default Comment;
